# For all the subpackages
/**/node_modules/

# Comment out if you wish to share the access values via version control.
/firebase.*.js

# Temporary
#/first/.captured.sdkconfig
/first/.state/

# IDEs (these could be in a global exclude)
/.vscode
