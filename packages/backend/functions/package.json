{
  "name": "functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=16"
  },
  "files": [
    "src/**/*.js",
    "index.js"
  ],
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "@google-cloud/logging": "^9.8.0",
    "firebase-admin": "^10.0.2",
    "firebase-functions": "^3.19.0"
  },
  "//": {
    "engines.node": [
      "Firebase emulators are not able to chew other valid Node.js version definitions, except plain '16' and such,",
      "so the repo makes a *temporary copy* of this file, with simpler 'engines.node', each time used. Clumsy, but..",
      "..what else can one do?"
    ],
    "scripts": {
      "@google-cloud/functions-framework": [
        "Included, since a post by Firebase suggests that (explicitly defining the version), moving to ES modules implementation (past 3.14.1)."
      ],
      "firebase-functions": [
        "!3.15.2 - that was a bad one."
      ]
    }
  }
}
