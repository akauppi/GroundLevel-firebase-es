{
  "name": "functions",
  "private": true,
  "//type": "module",
  "engines": {
    "node": "14 || >=15"
  },
  "dependencies": {
    "@google-cloud/logging": "^9.1.0",
    "firebase-admin": "^9.5.0",
    "firebase-functions": "^3.13.2"
  },
  "//": {
    "type": [
      "Change to 'type: module' when Firebase is ready for it (not yet in Feb 2021)."
    ],
    "engines.node": [
      "We prefer e.g. \"14 || >=15\". During deployment, the scripts turn this into \"14\", which is acceptable to Firebase CLI (9.4.0)",
      "",
      ">= 13.2 provides native ES module support"
    ]
  }
}
