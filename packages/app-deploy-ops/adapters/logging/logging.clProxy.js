/*
* src/logging/logging.googleCloudLogging.js
*/

// There is no browser client for Google Cloud Logging; otherwise we'd use one.

import { logger } from 'src/logging/logging.googleCloudLogging'

const logging = [logger];

export {
  logging
}
