{
  "description": "Sample front end (commands executed within DC)",
  "private": true,
  "type": "module",
  "scripts": {
    "dc:launch:local": "npm run -s _genEnvLocal && vite --mode dev_local",
    "dc:launch:online": "npm run -s _genEnvOnline && vite --mode dev_online",
    "-- ": "",
    "dc:launch:test": "npm run -s _genEnvLocal && vite --mode dev_local",
    "-- -": "",
    "_genEnvLocal": "FIREBASE_APP_JS=firebase.app.js tools/gen-vite-env-local.js --project=demo-main > .env",
    "_genEnvOnline": "tools/gen-vite-env-online.js > .env"
  },
  "//comments": {
    "": [
      "npm/node commands used from Docker Compose files. Keeping them here makes the root 'package.json' simpler."
    ],
    "_genEnv": [
      "Creates a '.env' file and after that, the mode used by 'dc:launch' ('--mode dev_local') doesn't matter, any more..",
      "^-- tbd. is that so??"
    ]
  }
}
