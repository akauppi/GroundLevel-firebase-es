{
  "name": "@local/app",
  "description": "Sample front end (development, build)",
  "license": "MIT",
  "private": true,
  "author": "<akauppi@gmail.com>",
  "type": "module",
  "files": [
    "vitebox/dist/*"
  ],
  "exports": {
    ".": "./vitebox/dist/app.es.js"
  },
  "engines": {
    "node": ">=14.3 || ^15"
  },
  "--": "-- following stuff matters for development, only --",
  "scripts": {
    "dev": "npm run dev:local",
    "__": "",
    "test": "if port-is-free --silent 3000 ; then npm run _test_ci ; else npm run --silent _test_dev ; fi",
    "_test_ci": "concurrently --kill-others --success=first -n dev,cy \"npm run dev:local\" \"wait-for 3000 && cypress run\"",
    "_test_dev": "( >&2 echo \"Found 'npm run dev' already running -- good..\" ) && cypress run",
    "  -": "",
    "dev:local": "concurrently --kill-others-on-fail -n emul,init \"firebase emulators:start --project=app --only auth,functions,firestore\" \"npm run --silent _dev_local_init && npm run --silent _dev_local_vite\"",
    "_dev_local_init": "wait-for 4000 && GCLOUD_PROJECT=app firebase-prime local/docs.js local/users.js",
    "_dev_local_vite": "vite --port 3000 --mode dev_local --clearScreen false",
    "  --": "",
    "dev:online": "concurrently --kill-others-on-fail -n log,vite \"npm run --silent _dev_online_log\" \"npm run --silent _dev_online_vite\"",
    "_dev_online_log": "PORT=5050 node tools/printout.js",
    "_dev_online_vite": "wait-for 5050 && vite --port 3001 --clearScreen false",
    "  ---": "",
    "lint": "eslint --ext .js,.vue src",
    "build": "vite build",
    "//": "which firebase",
    "  - ": "",
    "prebuild": "npm run --silent _checkNoLinks",
    "predev:local": "port-is-free 3000,5002 && npm run --silent _genEnvLocal",
    "predev:online": "port-is-free 3001 && npm run --silent _genEnvOnline",
    "_checkNoLinks": "tools/check-no-links.sh || ( >&2 echo \"ERROR: 'npm' links not allowed in a production build. Use them only at development.\n\tUse 'npm list' to see them; 'npm unlink x; npm install' to remove.\n\"; exit 8)",
    "_genEnvLocal": "node --harmony-top-level-await --experimental-json-modules tools/gen-vite-env-local.js firebase.json > vitebox/.env.dev_local",
    "_genEnvOnline": "node --harmony-top-level-await tools/gen-vite-env-online.js > vitebox/.env.development",
    "//_checkActivation": "check-activation || ( >&2 echo \"ERROR: No active project. Please run 'npm run activate' at project root.\n\"; exit 1)"
  },
  "dependencies": {
    "@firebase/app": "file:../../node_modules/@firebase/app",
    "@firebase/auth": "file:../../node_modules/@firebase/auth",
    "@firebase/firestore": "file:../../node_modules/@firebase/firestore",
    "@firebase/functions": "file:../../node_modules/@firebase/functions",
    "aside-keys": "^0.0.2-alpha.5",
    "vue": "^3.0.11",
    "vue-router": "^4.0.6"
  },
  "devDependencies": {
    "@local/backend": "file:../backend",
    "@local/firebase-prime": "file:../../tools/firebase-prime",
    "@local/tools": "file:../../tools",
    "@vitejs/plugin-vue": "^1.2.2",
    "@vue/compiler-sfc": "^3.0.11",
    "concurrently": "^6.0.2",
    "cypress": "^7.1.0",
    "eslint": "file:../../node_modules/eslint",
    "eslint-plugin-cypress": "^2.11.2",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-vue": "^7.9.0",
    "prettier": "^2.2.0",
    "sass": "^1.32.11",
    "vite": "^2.2.3",
    "vue-eslint-parser": "^7.6.0"
  },
  "//comments": {
    "scripts": {
      "test": [
        "Made so that if 'npm run dev' is running, it automatically uses that. Otherwise (in CI/CD) launches and shuts down the emulators."
      ],
      "predev:local": [
        "'firebase' CLI version (>= 8.15.0) matters for the use of auth emulation."
      ]
    },
    "devDependencies": {
      "@vue/compiler-sfc": [
        "This needs to be the same version as 'dependencies.vue' but did not find a way to enforce that."
      ],
      "prettier": [
        "tbd. why do we have it? Keep, but mention here."
      ],
      "sass": [
        "Needed as a peer dep, since we use '<style scoped lang=scss>' in the application. Otherwise, there's a RED dialog",
        "in the browser: '[plugin:vite:css] Preprocessor dependency \"sass\" not found. Did you install it?\n'"
      ]
    },
    "engines": [
      "'>=14.3' for '—-harmony-top-level-await'",
      "'>=13.2' for ES module support",
      "'--experimental-json-modules' seems to be available for Node 12"
    ],
    "peerDependencies": {
      "/not/ firebase": [
        "It's important we *don't* list 'firebase' as a peer dependency, though mentally it is. Because:",
        "- we don't want to mention the version twice (only at the root level)",
        "- we'd force 'app-ops-deploy' to pull in *all* of 'firebase' (and it would likely do it locally, which is unnecessary)",
        "",
        "Instead, root defines the version and both us and 'app-ops-deploy' pull in the necessary '@firebase' modules, directly."
      ]
    }
  }
}
