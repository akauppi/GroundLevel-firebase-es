//
// No access
//
service cloud.firestore {
  match /databases/{database}/documents {

    match /{document=**} {
      allow read: if false;
      allow write: if false;
    }

    match /abc/{id} {
      allow read: if true;
      allow write: if false;
    }
  }
}
