{
  "name": "rules-test",
  "version": "0.0.0",
  "description": "Rules tests",
  "private": true,
  "type": "module",
  "jest": {
    "globalSetup": "./setup.jest.js",
    "//": {
      "//testTimeout": "default 5000 _per test_ is mostly fine. After updates of dependencies, it still was too slooow."
    }
  },
  "scripts": {
    "test": "firebase emulators:exec --only firestore 'npx jest --verbose'",
    "test-dev": "FIRESTORE_EMULATOR_HOST=localhost:6767 npx jest --verbose"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.10.3",
    "@firebase/testing": "^0.20.4",
    "babel-jest": "^25.2.7",
    "jest": "^25.2.7"
  },
  "//": {
    "babel*": [
      "Babel is needed (Mar 2020) to have Jest support ES6 module imports/exports",
      "see -> https://medium.com/@saplos123456/using-es6-import-and-export-statements-for-jest-testing-in-node-js-b20c8bd9041c"
    ],
    "@firebase/testing": [
      "see -> https://github.com/firebase/firebase-js-sdk/tree/master/packages/testing"
    ]
  },
  "dependencies": {
    "@babel/preset-typescript": "^7.9.0"
  }
}
