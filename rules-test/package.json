{
  "name": "rules-test",
  "version": "0.0.0",
  "description": "Rules tests",
  "private": true,
  "//type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore 'npx jest --verbose'",
    "test-projects": "firebase emulators:exec --only firestore 'npm run _test-projects-npx'",
    "_test-projects-npx": "npx jest -f projectsC.test.js -t \"'/projects' rules\"",
    "test-symbols": "firebase emulators:exec --only firestore 'npm run _test-symbols-npx'",
    "_test-symbols-npx": "npx jest -f symbolsC.test.js -t \"'/symbols' rules\"",
    "test-invites": "firebase emulators:exec --only firestore 'npm run _test-invites-npx'",
    "_test-invites-npx": "npx jest -f invitesC.test.js -t \"'/invites' rules\"",
    "test-projects:dev": "FIRESTORE_EMULATOR_HOST=localhost:6767 npx jest -f projectsC.test.js -t \"'/projects' rules\" --detectOpenHandles",
    "test-symbols:dev": "FIRESTORE_EMULATOR_HOST=localhost:6767 npx jest -f symbolsC.test.js -t \"'/symbols' rules\" --detectOpenHandles",
    "test-invites:dev": "FIRESTORE_EMULATOR_HOST=localhost:6767 npx jest -f invitesC.test.js -t \"'/invites' rules\" --detectOpenHandles",
    "test-dev": "FIRESTORE_EMULATOR_HOST=localhost:6767 npx jest --verbose",
    "_start": "firebase emulators:start --only firestore"
  },
  "dependencies": {
    "@babel/preset-typescript": "^7.10.4"
  },
  "devDependencies": {
    "@babel/core": "^7.10.5",
    "@babel/preset-env": "^7.10.4",
    "@firebase/testing": "^0.20.6",
    "babel-jest": "^26.1.0",
    "jest": "^26.1.0"
  },
  "engines": {
    "node": ">=13.2"
  },
  "//": {
    "babel*": [
      "Babel is needed (Mar 2020) to have Jest support ES6 module imports/exports",
      "see -> https://medium.com/@saplos123456/using-es6-import-and-export-statements-for-jest-testing-in-node-js-b20c8bd9041c"
    ],
    "@firebase/testing": [
      "see -> https://github.com/firebase/firebase-js-sdk/tree/master/packages/testing"
    ],
    "Node 13.2+": [
      "Needed for native ES module support"
    ]
  }
}
