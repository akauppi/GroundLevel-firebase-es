{
  "name": "rules-test",
  "version": "0.0.0",
  "description": "Rules tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test4": "firebase emulators:exec --only firestore 'npx jest --config jest.config.js'",
    "test3": "firebase emulators:exec --only firestore 'npx --node-arg --experimental-vm-modules jest --config jest.config.js'",
    "test2": "firebase emulators:exec --only firestore 'node --experimental-vm-modules node_modules/jest/bin/jest.js --config jest.config.js'",
    "//test": "firebase emulators:exec --only firestore 'npx jest --config jest.config.js'",
    "//test-dev": "FIRESTORE_EMULATOR_HOST=localhost:6767 npx jest --config jest.config.js"
  },
  "dependencies": {
    "@babel/preset-typescript": "^7.9.0"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.10.3",
    "@firebase/testing": "^0.20.4",
    "@jest/globals": "^26.1.0",
    "babel-jest": "^25.5.1",
    "jest": "^26.1.0"
  },
  "engines": {
    "node": ">=13.2"
  },
  "//": {
    "babel*": [
      "Babel is needed (Mar 2020) to have Jest support ES6 module imports/exports",
      "see -> https://medium.com/@saplos123456/using-es6-import-and-export-statements-for-jest-testing-in-node-js-b20c8bd9041c"
    ],
    "@firebase/testing": [
      "see -> https://github.com/firebase/firebase-js-sdk/tree/master/packages/testing"
    ],
    "test": [
      "Once Node doesn't need the '--experimental-vm-modules', change the 'engines' requirement and run with 'npx jest'."
    ],
    "Node 13.2+": [
      "Needed for native ES module support"
    ]
  }
}
