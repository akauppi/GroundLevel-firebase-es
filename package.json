{
  "name": "@local/root",
  "private": true,
  "type": "module",
  "engines": {
    "node": "14 || >= 16 < 16.6 || >= 16.6.1"
  },
  "scripts": {
    "first": "first/deploy.sh",
    "--": "",
    "preinstall": "[ $BUILDER_OUTPUT ] || sh/npm-version-check.sh",
    "postinstall": "npm --prefix tools install"
  },
  "devDependencies": {
    "eslint": "^8.7.0",
    "eslint-plugin-node": "^11.1.0",
    "firebase": "^9.6.4"
  },
  "//": {
    "": [
      "This serves for pulling in libraries and tools used commonly in the subpackages, so that a) version choice is in",
      "one place and b) we save both download bandwidth and disk space.",
      "",
      "Some tools ('wait-on', 'concurrently') are not here. Those are seen as implementation details of the particular",
      "submodules (so if you use a package in two subpackages, it does not mean you must declare it here)."
    ],
    "engines.node": [
      "Demanding it here provides the developer instant feedback if the Node.js is not suitable.",
      "16.6.0 has an issue with Jest that's fixed in 16.6.1."
    ],
    "scripts": {
      "$BUILDER_OUTPUT": [
        "Indication of Cloud Build - defined both in automated and manually triggered builds.",
        "Avoid '[[' (bash) since 'node:16-alpine' image (CI) won't have it."
      ]
    }
  }
}
