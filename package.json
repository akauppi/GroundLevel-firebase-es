{
  "name": "groundlevel-es6-firebase-web",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/akauppi/groundlevel-es6-firebase-web.git"
  },
  "author": "<akauppi@gmail.com>",
  "private": true,
  "scripts": {
    "dev": "vite --port 3000",
    "dev:local": "firebase emulators:exec --only functions,firestore \"vite --port 3001\"",
    "build": "vite build",
    "clean": "(rm public/dist/bundle* 2> /dev/null || true)"
  },
  "//scripts": {
    "rollup:dev": "concurrently --restart-tries 3 --names rollup,firebase-serve \"rollup -c -w\" \"firebase serve --port 3000\"",
    "rollup:build": "rollup -c"
  },
  "dependencies": {
    "firebase": "^7.14.5",
    "vue": "^3.0.0-beta.15",
    "vue-router": "^4.0.0-alpha.13"
  },
  "//dependencies": {
    "bootstrap": "^4.5.0",
    "jquery": "^3.5.1",
    "popper.js": "^1.16.1"
  },
  "//devDependencies": {
    "//@firebase/testing": "^0.19.5",
    "//@rollup/plugin-commonjs": "^11.0.1",
    "//@rollup/plugin-replace": "^2.3.1"
  },
  "//devDependencies.2": {
    "": "Currently put aside. See if we want to use Rollup for build (or 'vite build')",
    "@rollup/plugin-alias": "^3.1.0",
    "@rollup/plugin-node-resolve": "^7.1.3",
    "rollup": "^2.6.1",
    "rollup-plugin-eslint": "^7.0.0",
    "rollup-plugin-filesize": "^7.0.0",
    "rollup-plugin-livereload": "^1.2.0",
    "rollup-plugin-terser": "^5.3.0",
    "rollup-plugin-vue": "^6.0.0-alpha.5"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.0-beta.15",
    "concurrently": "^5.0.2",
    "eslint": "^7.3.0",
    "eslint-plugin-vue": "^7.0.0-alpha.6",
    "jest": "^25.3.0",
    "sass": "^1.26.8",
    "vite": "0.20.10"
  },
  "//comments": {
    "devDependencies": {
      "@vue/compiler-sfc": [
        "This needs to be the same version as 'dependencies.vue' but did not find a way to ",
        "enforce that. Tried '$npm_package_dependencies_vue' but that doesn't work. npm 6.14.4 on macOS."
      ]
    },
    "scripts": {
      "build": [
        "Builds production code"
      ],
      "dev:local": [
        "Unfortunately, 'local' is a special word for Vite ('--mode local' should not be allowed since any invocation",
        "loads '.env.local'. Using a misspelled variant to avoid this."
      ]
    }
  }
}
