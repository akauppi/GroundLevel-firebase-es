{
  "name": "@local/root",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">= 16 < 16.6 || >= 16.6.1 < 17 || 18"
  },
  "scripts": {
    "first": "(cd first && ./deploy.sh)"
  },
  "devDependencies": {
    "eslint": "^8.17.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-cypress": "^2.12.1",
    "eslint-plugin-vue": "^9.1.1",
    "vue-eslint-parser": "^9.0.3"
  },
  "//": {
    "": [
      "This serves for pulling tools used commonly in the subpackages, so that the version choice is in one place."
    ],
    "engines.node": [
      "Demanding it here provides the developer instant feedback if the Node.js version is not suitable.",
      "16.6.0 has an issue with Jest that's fixed in 16.6.1."
    ]
  }
}
