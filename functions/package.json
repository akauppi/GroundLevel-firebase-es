{
  "name": "functions",
  "private": true,
  "//type": "module",
  "//scripts": {
    "//serve": "firebase emulators:start --only functions",
    "//shell": "firebase functions:shell",
    "//start": "npm run shell",
    "//deploy": "firebase deploy --only functions",
    "//logs": "firebase functions:log"
  },
  "engines": {
    "node": "12"
  },
  "dependencies": {
    "firebase-admin": "^8.13.0",
    "firebase-functions": "^3.8.0"
  },
  "//": {
    "engines.node": [
      "Cloud Functions only use LTS versions (even) of Node, e.g. 12,14,...",
      "Native ES module support has been opened in Node.js 12.x.x  (tbd!!!!!!!!!!)"
    ]
  }
}
