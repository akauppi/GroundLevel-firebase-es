const base = require('./vite.config.js');

module.exports = { ...base,

  /* Unfortunately this doesn't work (Vite 0.20.10)
  // Run towards the local emulators
  env: {
    DEV_FUNCTIONS_URL: "http://localhost:5001",
    DEV_FIRESTORE_HOST: "localhost:8080"
  }
  */
}
