{
  "hosting": {
    "public": "public",
    "ignore": [],
    "rewrites": [
      {
        "//": "Keep this first! '.js' and '.css' are not redirected.",
        "source": "**/*.@(css|js)",
        "destination": "/nosuch"
      },
      {
        "//": "Single-page-app (keep last)",
        "source": "**",
        "destination": "/index.prod.html"
      }
    ],
    "//": {
      "Intention": [
        "Serve the same page for everything '.html' or postfixless: client does the routing.",
        "Note: if we let pass here, it's going to be a 200. Handle obvious 404's like wrong path to scripts or CSS here."
      ],
      "Creating 404": [
        "Directing to a non-existing page causes the response to become 404 (this is undocumented behavior; an alternative",
        "could be excluding some suffixes from SPA route, but 'glob' syntax doesn't sport a NOT operator, does it? #help"
      ]
    },
    "headers": [
      {
        "//": "Firebase (7.15.5) provides wrong default MIME type for '.js' (application/json)",
        "source": "**/*.js",
        "headers": [
          {
            "key": "Content-Type",
            "value": "application/javascript"
          }
        ]
      }
    ]
  },
  "firestore": {
    "rules": "back-end/firestore.rules",
    "indexes": "back-end/firestore.indexes.json"
  },
  "emulators": {
    "firestore": {
      "port": "6767"
    }
  }
}
